// Устанавливаем имя корневого проекта
rootProject.name = "grok-android-1.0.88-release.00"

// Включаем подпроект 'app'.
// Обрати внимание: имя директории 'app' соответствует имени модуля.
include(":app")
